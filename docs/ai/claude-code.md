# Embod With Claude Code

Embod is exposed as a local CLI with deterministic outputs.

- Start with `embod capabilities --json`
- Inspect and validate before exporting
- Use `snapshot --json` to verify that intent, geometry, and rendered reality match
- Prefer updating `embod_project.py` directly instead of generating separate wrappers
