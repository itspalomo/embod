expected_project_name: placement_failures_case
expected_parts:
  - fragile_plate
required_diagnostic_codes:
  - placement.insufficient_clearance
  - placement.insufficient_thickness
required_export_formats:
  - stl
